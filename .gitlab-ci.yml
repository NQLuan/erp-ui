# Author: Sang Nguyen
# Date: Apr 1 2020
# Description: add gitlab-ci configuration file


before_script:
  - apt-get update -qq
  - apt-get install -qq git
  # Setup SSH deploy keys
  - 'which ssh-agent || ( apt-get install -qq openssh-client )'
  - eval $(ssh-agent -s)
  - ssh-add <(echo "$SSH_PRIVATE_KEY")
  - mkdir -p ~/.ssh
  - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
    
deploy_staging:
  type: deploy
  environment:
    name: staging
    url: 35.209.247.237
  script:
    - ssh desktop-win-sang@35.209.247.237 "cd ~/workspace/company-ui && git checkout master && git pull origin master && exit"
  only:
    - master